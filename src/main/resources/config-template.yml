# KataParty Configuration, generated by [%@version%]

# This is what shoud be at the start of a message to send to the opposite
# chat, global instead of party or vice-versa
chat-filtering-swap: "[%chat-filtering-swap%]"

# Below, you can customize the messages that are sent to players
messages:
  blah: "[%messages.blah%]"
